name: Final Schedule Test
# 格式A：明确设置为空映射（推荐，最清晰）
on:
  schedule:
    - cron: '55 20 * * *'
  workflow_dispatch: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Mark Success
        run: echo "🟢 SUCCESS: Schedule triggered at $(date)"
